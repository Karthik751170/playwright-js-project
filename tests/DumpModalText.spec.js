const { test, expect } = require('@playwright/test');
test('Dump buttons', async ({ browser }) => {
    // I can't just run this without an active session! I need to do it inside the actual test!
});
